'use client'

import MarkdownEditor from './MarkdownEditor'
import type { AutoLinkEditorProps } from '@/types'

export default function AutoLinkEditor(props: AutoLinkEditorProps) {
  return <MarkdownEditor {...props} />
}
